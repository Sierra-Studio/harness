# Runtime (real harness). The core logic + offline demo/tests run WITHOUT these,
# falling back gracefully (heuristic tokenizer, local embeddings, in-memory repo).
httpx>=0.27          # OpenRouter HTTP client
psycopg[binary]>=3.1 # Postgres driver (real repository)
tiktoken>=0.7        # accurate token counting (optional; heuristic fallback exists)

# Dev
pytest>=8.0
